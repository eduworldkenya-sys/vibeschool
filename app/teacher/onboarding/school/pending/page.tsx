"use client"
import {useEffect,useState} from "react"
import {useRouter} from "next/navigation"
import {supabase} from "@/lib/supabase"
type Claim={reference_code?:string;status?:string;school_name?:string;review_note?:string}
export default function PendingSchoolClaim(){const router=useRouter();const [claim,setClaim]=useState<Claim|null>(null);const [loading,setLoading]=useState(true);useEffect(()=>{void(async()=>{const {data}=await supabase.rpc("get_my_teacher_school_claim");const c=(data||{}) as Claim;setClaim(c);setLoading(false);if(c.status==="approved")router.replace("/teacher/classhub")})()},[router]);return <main style={{minHeight:"100vh",background:"#f3f4f6",padding:16,fontFamily:"system-ui"}}><section style={{maxWidth:520,margin:"48px auto",background:"#fff",borderRadius:20,padding:24,boxShadow:"0 4px 20px rgba(0,0,0,.08)"}}><h1 style={{marginTop:0}}>School verification</h1>{loading?<p>Checking your claim…</p>:!claim?.status?<><p>No active school claim was found.</p><button onClick={()=>router.replace("/teacher/onboarding/school")}>Choose school</button></>:<><p><b>{claim.school_name||"Your school"}</b></p><p>Status: <b>{claim.status.replace("_"," ")}</b></p>{claim.reference_code&&<p style={{color:"#667085"}}>Reference: {claim.reference_code}</p>}{claim.status==="needs_information"&&<p role="alert">{claim.review_note||"The reviewer needs more information before approval."}</p>}{claim.status==="rejected"&&<button onClick={()=>router.replace("/teacher/onboarding/school")}>Choose another school</button>}{claim.status==="pending"&&<p style={{color:"#667085",lineHeight:1.5}}>Your selection has not granted school authority. An authorized school administrator or platform owner must approve it first.</p>}</>}</section></main>}
