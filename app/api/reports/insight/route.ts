import { randomUUID } from 'node:crypto'
import { NextRequest,NextResponse } from 'next/server'
import { createAnthropicMessagesAdapter,invokeGovernedCyborgModel } from '@/lib/cyborg/gateway'
export async function POST(req:NextRequest){try{const{reportType,data}=await req.json();if(!reportType||!data)return NextResponse.json({error:'Missing reportType or data'},{status:400});const summary=JSON.stringify(data).slice(0,3000),adapter=createAnthropicMessagesAdapter(),missionId=req.headers.get('x-cyborg-mission-id')?.trim()||`report-insight:${randomUUID()}`;const response=await invokeGovernedCyborgModel(adapter,{provider:'anthropic',model:'claude-sonnet-4-20250514',missionId,metadata:{maxTokens:150,feature:'report-insight'},messages:[{role:'user',content:`You are an AI analyst for a Kenyan school management system (CBC curriculum). Analyze this ${reportType} report and return one concise actionable insight (max 25 words). Data: ${summary}`}]});const payload=response.output as{content?:Array<{type?:string;text?:string}>},insight=payload.content?.find(i=>i.type==='text')?.text?.trim();if(!insight)throw new Error('CYBORG_PROVIDER_EMPTY_OUTPUT');return NextResponse.json({insight,missionId,cyborgCapability:response.capabilityJti})}catch(err){console.error('AI insight error:',err);return NextResponse.json({insight:'Unable to generate insight at this time.'},{status:200})}}
