"use client"
import {useCallback,useEffect,useState} from "react"
import {hqSupabase} from "@/lib/hq/supabase"
import {HQPage,HQPanel,HQ_THEME as C} from "@/components/hq/HQShell"
type U={id:string;full_name:string|null;role:string|null;account_status:string;is_anonymized:boolean;created_at:string;school_id:string|null;vc_id:string|null;active_subscription_count:number}
export default function HQUsers(){const [users,setUsers]=useState<U[]>([]),[q,setQ]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=useCallback(async()=>{setError("");const {data,error}=await hqSupabase.rpc("hq_user_directory",{p_search:q||null,p_limit:200});if(error){setError(error.message);return}setUsers((data||[]) as U[])},[q]);useEffect(()=>{void(async()=>{await load();setLoading(false)})()},[load]);return <HQPage title="Users & governance" description="Operational account status, school affiliation and subscription state. Owner-gated and privacy-minimized." actions={<button onClick={()=>void load()} style={{padding:"9px 12px",borderRadius:9,border:`1px solid ${C.border}`,background:"transparent",color:C.text}}>Refresh</button>}><HQPanel><div style={{padding:14}}><input aria-label="Search users" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')void load()}} placeholder="Search name or VibeSchool ID" style={{width:"100%",boxSizing:"border-box",padding:10,borderRadius:9,border:`1px solid ${C.border}`,background:C.panel,color:C.text}}/></div></HQPanel><div style={{height:12}}/><HQPanel title="Registered accounts">{error?<div style={{padding:14,color:C.red}}>{error}</div>:loading?<div style={{padding:14,color:C.muted}}>Loading…</div>:users.length===0?<div style={{padding:14,color:C.muted}}>No users found.</div>:users.map(u=><div key={u.id} style={{padding:"12px 14px",borderTop:`1px solid ${C.border}`,display:"grid",gridTemplateColumns:"2fr 1fr 1fr 1fr",gap:10,fontSize:12}}><div><b>{u.full_name||"Unnamed user"}</b><div style={{color:C.muted,fontSize:10}}>{u.vc_id||u.id}</div></div><div>{u.role||"—"}</div><div style={{color:u.account_status==='active'?C.green:C.amber}}>{u.account_status}</div><div>{u.active_subscription_count>0?`${u.active_subscription_count} billing record(s)`:"No active billing"}</div></div>)}</HQPanel></HQPage>}
