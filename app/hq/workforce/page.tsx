"use client"
export const dynamic="force-dynamic"

import {useCallback,useEffect,useMemo,useState} from "react"
import {supabase} from "@/lib/supabase"
import {HQPage,HQPanel,HQ_THEME as C,hqButtonStyle} from "@/components/hq/HQShell"

type Obj=Record<string,any>
type Snapshot={generated_at:string;engine:Obj|null;counts:Obj;workers:Obj[];jobs:Obj[];shadow_candidates:Obj[];shadow_runs:Obj[];decisions:Obj[];skills:Obj[];authority:Obj[];evidence:Obj[];failures:{anomalies:Obj[];dead_letters:Obj[]};resources:Obj[]}
type OpsSnapshot={generated_at:string;engine:Obj;global_policy:Obj|null;authority:Obj[];breakers:Obj[];executions:Obj[];budgets:Obj[];control_events:Obj[];counts:Obj}
type Tab="Control Room"|"Workers"|"Jobs"|"Shadow Runs"|"Decisions"|"Skills"|"Authority"|"Evidence"|"Failures"|"Resources"
const sb=supabase as any
const tabs:Tab[]=["Control Room","Workers","Jobs","Shadow Runs","Decisions","Skills","Authority","Evidence","Failures","Resources"]
const yes=(v:any)=>v===true
const tone=(kind:string)=>kind==="bad"?C.red:kind==="warn"?C.amber:kind==="good"?C.green:C.blue
const when=(v:any)=>v?new Date(String(v)).toLocaleString("en-KE"):"—"
const short=(v:any,n=90)=>{const s=typeof v==="string"?v:JSON.stringify(v??{});return s.length>n?s.slice(0,n)+"…":s}

function Badge({children,kind="info"}:{children:any;kind?:"info"|"good"|"warn"|"bad"}){return <span style={{display:"inline-flex",alignItems:"center",minHeight:24,padding:"0 8px",borderRadius:999,border:`1px solid ${tone(kind)}44`,background:`${tone(kind)}12`,color:tone(kind),fontSize:10,fontWeight:900}}>{children}</span>}
function Metric({label,value,kind="info"}:{label:string;value:any;kind?:"info"|"good"|"warn"|"bad"}){return <div style={{padding:13,border:`1px solid ${C.border}`,borderRadius:14,background:C.panelSoft}}><div style={{fontSize:22,fontWeight:950,color:tone(kind)}}>{String(value??0)}</div><div style={{marginTop:4,fontSize:10.5,color:C.muted}}>{label}</div></div>}
function Rows({items,render,empty="No records."}:{items:Obj[];render:(x:Obj)=>React.ReactNode;empty?:string}){return <div style={{display:"grid",gap:8}}>{items.length?items.map((x,i)=><div key={String(x.id??x.trace_id??x.worker_key??x.skill_key??i)} style={{padding:12,borderBottom:i===items.length-1?"none":`1px solid ${C.border}`}}>{render(x)}</div>):<div style={{padding:24,textAlign:"center",fontSize:11.5,color:C.muted}}>{empty}</div>}</div>}
function ActionBox({title,children}:{title:string;children:React.ReactNode}){return <div style={{padding:14,border:`1px solid ${C.border}`,borderRadius:14,background:C.panelSoft}}><div style={{fontSize:12,fontWeight:900,marginBottom:9}}>{title}</div>{children}</div>}

export default function WorkerEngineControlRoom(){
 const[data,setData]=useState<Snapshot|null>(null);const[ops,setOps]=useState<OpsSnapshot|null>(null);const[tab,setTab]=useState<Tab>("Control Room");const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[actionBusy,setActionBusy]=useState(false);const[actionMessage,setActionMessage]=useState("");const[showStart,setShowStart]=useState(false)
 const load=useCallback(async()=>{setLoading(true);setError("");try{const[{data:base,error:baseError},{data:operations,error:opsError}]=await Promise.all([sb.rpc("hq_workforce_get_control_room_snapshot",{p_recent_limit:50}),sb.rpc("hq_workforce_owner_control_snapshot",{p_recent_limit:50})]);if(baseError)throw baseError;setData(base as Snapshot);if(opsError){setOps(null);setError(`Operational controls are not commissioned on this environment: ${opsError.message}`)}else setOps(operations as OpsSnapshot)}catch(e){setError(e instanceof Error?e.message:"Worker Engine Control Room could not be loaded.")}finally{setLoading(false)}},[])
 useEffect(()=>{void load()},[load])
 const e=ops?.engine??data?.engine??{}
 const globalStop=yes(e.shadow_global_stop)
 const runtimeOn=yes(e.runtime_execution_enabled)
 const safety=useMemo(()=>({runtimeOff:!runtimeOn&&Number(e.runtime_autonomy_level??0)===0,shadowStopped:!yes(e.shadow_enabled)&&!yes(e.shadow_scheduler_enabled)&&globalStop,factoryOff:!yes(e.factory_enabled),heartbeatOff:!yes(e.heartbeat_enabled)}),[e,globalStop,runtimeOn])
 const activeAuthority=Number(ops?.counts?.active_authority??0)
 const globalPolicy=ops?.global_policy
 const canStart=Boolean(ops)&&!runtimeOn&&!globalStop&&yes(globalPolicy?.enabled)&&activeAuthority>0&&Number(globalPolicy?.max_autonomy_level??0)>0
 const startAutonomy=Math.max(1,Math.min(1,Number(globalPolicy?.max_autonomy_level??1)))
 const startRisk=Math.max(0,Math.min(1,Number(globalPolicy?.max_risk_class??1)))
 async function review(id:string,state:"approved"|"rejected"|"revise"){const rationale=window.prompt(state==="approved"?"Optional review rationale":"Review rationale")??"";if(state!=="approved"&&!rationale.trim())return;setError("");const{error}=await sb.rpc("hq_workforce_owner_review_shadow_decision",{p_decision_id:id,p_state:state,p_rationale:rationale||null});if(error){setError(error.message);return}await load()}
 async function ownerAction(kind:"start"|"stop"|"global-on"|"global-off"){
  if(!ops)return
  const labels={start:"Start controlled operations",stop:"Stop operations","global-on":"ACTIVATE GLOBAL STOP","global-off":"Release Global Stop"} as const
  const reason=window.prompt(`${labels[kind]} — enter an audit reason`)
  if(!reason?.trim())return
  const confirmation=kind==="global-on"?"GLOBAL STOP":kind==="start"?"START":"CONFIRM"
  if(window.prompt(`Type ${confirmation} to confirm`)!==confirmation)return
  setActionBusy(true);setError("");setActionMessage("")
  try{
   let response:any
   if(kind==="start") response=await sb.rpc("hq_workforce_owner_start_controlled_operations",{p_expected_updated_at:e.updated_at,p_autonomy_level:startAutonomy,p_max_risk:startRisk,p_reason:reason.trim()})
   else if(kind==="stop") response=await sb.rpc("hq_workforce_owner_stop_operations",{p_expected_updated_at:e.updated_at,p_reason:reason.trim()})
   else response=await sb.rpc("hq_workforce_owner_set_global_stop",{p_active:kind==="global-on",p_reason:reason.trim()})
   if(response.error)throw response.error
   setActionMessage(`${labels[kind]} completed and authoritative state was refreshed.`);setShowStart(false);await load()
  }catch(err){setError(err instanceof Error?err.message:String((err as any)?.message??err))}finally{setActionBusy(false)}
 }
 return <HQPage title="Worker Engine Control Room" description="Owner control plane for bounded Worker Engine operations, authority, evidence and emergency containment" actions={<button onClick={()=>void load()} disabled={loading||actionBusy} style={hqButtonStyle}>{loading?"Refreshing…":"Refresh state"}</button>}>
  {error&&<div role="alert" style={{marginBottom:12,padding:12,border:"1px solid rgba(251,113,133,.3)",borderRadius:11,background:"rgba(251,113,133,.08)",color:"#fecdd3",fontSize:11.5}}>{error}</div>}
  {actionMessage&&<div role="status" style={{marginBottom:12,padding:12,border:`1px solid ${C.green}44`,borderRadius:11,background:`${C.green}10`,color:C.green,fontSize:11.5}}>{actionMessage}</div>}
  <div style={{display:"flex",gap:6,overflowX:"auto",paddingBottom:10}}>{tabs.map(t=><button key={t} onClick={()=>setTab(t)} style={{...hqButtonStyle,minHeight:36,whiteSpace:"nowrap",color:tab===t?C.green:C.muted,borderColor:tab===t?`${C.green}55`:C.border}}>{t}</button>)}</div>
  {!data?<HQPanel><div style={{padding:28,color:C.muted,fontSize:12}}>{loading?"Loading authoritative Worker Engine state…":"No control-room snapshot available."}</div></HQPanel>:<>
   {tab==="Control Room"&&<div style={{display:"grid",gap:12}}>
    <HQPanel title="Safety summary" description="Authoritative backend posture. OFF is a valid safe operating state."><div style={{padding:14,display:"grid",gap:12}}>
      <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(128px,1fr))",gap:8}}><Metric label="Runtime" value={runtimeOn?"ON":"OFF"} kind={runtimeOn?"warn":"good"}/><Metric label="Autonomy" value={`L${e.runtime_autonomy_level??0}`} kind={Number(e.runtime_autonomy_level??0)>1?"warn":"info"}/><Metric label="Maximum risk" value={`R${e.runtime_max_risk??0}`} kind={Number(e.runtime_max_risk??0)>1?"warn":"info"}/><Metric label="Global Stop" value={globalStop?"ACTIVE":"INACTIVE"} kind={globalStop?"good":"warn"}/><Metric label="Active authority" value={activeAuthority} kind={activeAuthority?"warn":"good"}/><Metric label="Active workers" value={ops?.counts?.active_workers??data.counts.active_workers}/><Metric label="Open decisions" value={ops?.counts?.open_decisions??data.counts.decisions_waiting} kind={Number(ops?.counts?.open_decisions??data.counts.decisions_waiting)>0?"warn":"good"}/><Metric label="Tripped breakers" value={ops?.counts?.tripped_breakers??0} kind={Number(ops?.counts?.tripped_breakers??0)>0?"bad":"good"}/></div>
      <div style={{display:"flex",gap:8,flexWrap:"wrap"}}><Badge kind={safety.runtimeOff?"good":"warn"}>Runtime {safety.runtimeOff?"L0 / OFF":"OPERATING"}</Badge><Badge kind={safety.factoryOff?"good":"bad"}>Factory {safety.factoryOff?"OFF":"ON"}</Badge><Badge kind={safety.heartbeatOff?"good":"warn"}>Heartbeat {safety.heartbeatOff?"OFF":"ON"}</Badge><Badge kind={yes(e.runtime_anomaly_paused)?"warn":"good"}>Runtime anomaly pause {yes(e.runtime_anomaly_paused)?"ON":"clear"}</Badge><Badge kind={ops?"good":"warn"}>{ops?"Owner operation RPCs available":"Read-only fallback"}</Badge></div>
      <div style={{fontSize:10.5,color:C.muted}}>State version {when(e.updated_at)} · snapshot {when(ops?.generated_at??data.generated_at)}</div>
    </div></HQPanel>

    <HQPanel title="Owner operations" description="Consequential controls use server-side owner authorization, deliberate confirmation, stale-state checks and audit evidence."><div style={{padding:14,display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:10}}>
      <ActionBox title="Start Controlled Operations"><div style={{fontSize:10.5,color:C.muted,lineHeight:1.6}}>Requires Global Stop released, an enabled global policy and active temporary capability authority. It never creates authority automatically.</div><button disabled={!canStart||actionBusy} onClick={()=>setShowStart(true)} style={{...hqButtonStyle,width:"100%",marginTop:10,minHeight:42,color:canStart?C.green:C.muted}}>Review activation</button>{!canStart&&<div style={{marginTop:7,fontSize:10,color:C.amber}}>Unavailable until the governed envelope is complete.</div>}</ActionBox>
      <ActionBox title="Stop Operations"><div style={{fontSize:10.5,color:C.muted,lineHeight:1.6}}>Normal controlled shutdown: runtime → OFF/L0/R0 and active capability authority is suspended.</div><button disabled={!ops||!runtimeOn||actionBusy} onClick={()=>void ownerAction("stop")} style={{...hqButtonStyle,width:"100%",marginTop:10,minHeight:42,color:C.amber}}>Stop Operations</button></ActionBox>
      <ActionBox title="Global Stop"><div style={{fontSize:10.5,color:C.muted,lineHeight:1.6}}>Emergency containment: stops runtime and shadow scheduling, trips the global breaker and neutralizes active authority.</div>{globalStop?<button disabled={!ops||actionBusy} onClick={()=>void ownerAction("global-off")} style={{...hqButtonStyle,width:"100%",marginTop:10,minHeight:42,color:C.amber}}>Release Global Stop</button>:<button disabled={!ops||actionBusy} onClick={()=>void ownerAction("global-on")} style={{...hqButtonStyle,width:"100%",marginTop:10,minHeight:42,color:C.red}}>Activate Global Stop</button>}</ActionBox>
    </div></HQPanel>

    {showStart&&<HQPanel title="Activation review" description="Current state → proposed bounded state. No authority is silently created."><div style={{padding:14,display:"grid",gap:10}}><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(180px,1fr))",gap:8}}><ActionBox title="Runtime"><div style={{fontSize:11}}>OFF → <strong>ON</strong></div></ActionBox><ActionBox title="Autonomy"><div style={{fontSize:11}}>L{e.runtime_autonomy_level??0} → <strong>L{startAutonomy}</strong></div></ActionBox><ActionBox title="Maximum risk"><div style={{fontSize:11}}>R{e.runtime_max_risk??0} → <strong>R{startRisk}</strong></div></ActionBox><ActionBox title="Authority"><div style={{fontSize:11}}>{activeAuthority} active temporary grant{activeAuthority===1?"":"s"}; none created by activation.</div></ActionBox></div><div style={{fontSize:10.5,color:C.muted}}>Global policy permits up to L{globalPolicy?.max_autonomy_level??0} / R{globalPolicy?.max_risk_class??0}, concurrency {globalPolicy?.max_concurrency??"—"}, rate {globalPolicy?.max_executions_per_minute??"—"}/min. Runtime start still rechecks every backend precondition.</div><div style={{display:"flex",gap:8,flexWrap:"wrap"}}><button disabled={actionBusy} onClick={()=>void ownerAction("start")} style={{...hqButtonStyle,minHeight:42,color:C.green}}>Confirm Start Controlled Operations</button><button disabled={actionBusy} onClick={()=>setShowStart(false)} style={{...hqButtonStyle,minHeight:42}}>Cancel</button></div></div></HQPanel>}

    <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(280px,1fr))",gap:12}}>
      <HQPanel title="Temporary authority" description="What consequential capability is currently allowed, for whom, and until when."><Rows items={ops?.authority??[]} empty="No temporary authority. The engine has no active capability grant." render={x=><><div style={{display:"flex",gap:7,flexWrap:"wrap",alignItems:"center"}}><strong style={{fontSize:11.5}}>{x.capability_key}@{x.capability_version}</strong><Badge kind={x.status==="active"?"warn":x.status==="suspended"?"bad":"info"}>{x.status}</Badge><Badge>R{x.risk_class} · L{x.autonomy_level}</Badge></div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>{x.permitted_worker_key??"any permitted worker"} · {x.operation} {x.resource_type} · expires {when(x.expires_at)}</div></>}/></HQPanel>
      <HQPanel title="Circuit breakers" description="A tripped breaker is a prohibition. Recovery never restarts runtime automatically."><Rows items={ops?.breakers??[]} empty="No circuit-breaker history in the current window." render={x=><><div style={{display:"flex",gap:7,flexWrap:"wrap"}}><Badge kind={x.status==="tripped"?"bad":"good"}>{x.status}</Badge><strong style={{fontSize:11.5}}>{x.scope_type}: {x.scope_ref}</strong></div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>{x.reason_code} · tripped {when(x.tripped_at)}{x.reset_at?` · reset ${when(x.reset_at)}`:""}</div></>}/></HQPanel>
    </div>
    <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(280px,1fr))",gap:12}}>
      <HQPanel title="Recent executions" description="Immediate answer to what the Worker Engine actually attempted."><Rows items={ops?.executions??[]} empty="No governed execution records yet." render={x=><><div style={{display:"flex",gap:7,flexWrap:"wrap"}}><strong style={{fontSize:11.5}}>{x.worker_key}</strong><Badge>{x.capability_key}</Badge><Badge kind={x.status==="completed"?"good":x.status==="running"?"warn":x.status==="failed"||x.status==="dead_letter"?"bad":"info"}>{x.status}</Badge></div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>{x.operation} · attempts {x.attempt_count} · {when(x.completed_at??x.started_at??x.created_at)}</div>{x.last_error&&<div style={{marginTop:5,fontSize:10,color:C.red}}>{short(x.last_error,120)}</div>}</>}/></HQPanel>
      <HQPanel title="Resource envelope" description="Enforceable execution budgets only; no fabricated financial estimate."><Rows items={ops?.budgets??[]} empty="No execution budget window is configured." render={x=><><div style={{display:"flex",gap:7,flexWrap:"wrap"}}><strong style={{fontSize:11.5}}>{x.budget_key}</strong><Badge kind={x.status==="exhausted"?"bad":x.status==="active"?"good":"warn"}>{x.status}</Badge></div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>Consumed {x.consumed_amount} / {x.limit_amount} · reserved {x.reserved_amount} · ends {when(x.window_ends_at)}</div></>}/></HQPanel>
    </div>
    <HQPanel title="Owner audit trail" description="Consequential owner controls with previous, requested and resulting state."><Rows items={ops?.control_events??[]} empty="No Control Room owner actions recorded yet." render={x=><><div style={{display:"flex",gap:7,flexWrap:"wrap"}}><strong style={{fontSize:11.5}}>{x.action_key}</strong><Badge kind={x.outcome==="succeeded"?"good":x.outcome==="rejected"?"bad":"info"}>{x.outcome}</Badge></div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>{when(x.created_at)} · {short(x.reason,120)}</div></>}/></HQPanel>
   </div>}
   {tab==="Workers"&&<HQPanel title="Workers" description="Identity, lane and current lifecycle."><Rows items={data.workers} render={x=><><div style={{display:"flex",gap:7,alignItems:"center",flexWrap:"wrap"}}><strong style={{fontSize:12.5}}>{x.title}</strong><Badge kind={x.status==="active"?"good":x.status==="suspended"?"bad":"warn"}>{x.status}</Badge><Badge>{x.department_key}</Badge></div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>{x.worker_key} · {x.reasoning_mode} · paid AI {x.paid_ai_allowed?"allowed":"off"} · {when(x.updated_at)}</div></>}/></HQPanel>}
   {tab==="Jobs"&&<HQPanel title="Detected operational work" description="Source work remains unchanged by observation-only surfaces."><Rows items={data.jobs} render={x=><><div style={{display:"flex",gap:7,alignItems:"center",flexWrap:"wrap"}}><strong style={{fontSize:12}}>{x.title}</strong><Badge kind={x.priority==="critical"?"bad":x.priority==="high"?"warn":"info"}>{x.priority}</Badge><Badge>{x.status}</Badge></div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>{x.department_key} · {x.work_type} · due {when(x.due_at)}</div></>}/></HQPanel>}
   {tab==="Shadow Runs"&&<div style={{display:"grid",gap:12}}><HQPanel title="Candidate queue"><Rows items={data.shadow_candidates} render={x=><><div style={{display:"flex",gap:7,flexWrap:"wrap"}}><Badge>{x.lane_key}</Badge><Badge kind={x.status==="escalated"?"warn":"info"}>{x.status}</Badge><strong style={{fontSize:11.5}}>Priority {x.priority}</strong></div><div style={{marginTop:6,fontSize:10.5,color:C.muted}}>{short(x.reasoning_summary)} · confidence {x.confidence??"—"}</div></>}/></HQPanel><HQPanel title="Trace runs"><Rows items={data.shadow_runs} render={x=><><div style={{display:"flex",gap:7,flexWrap:"wrap"}}><strong style={{fontSize:11.5}}>{x.trace_id}</strong><Badge>{x.status}</Badge>{x.consequential_action_performed?<Badge kind="bad">CONSEQUENTIAL WRITE</Badge>:<Badge kind="good">No production action</Badge>}</div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>{x.worker_key} · {x.lane_key??"—"} · {when(x.started_at)}</div></>}/></HQPanel></div>}
   {tab==="Decisions"&&<HQPanel title="Decision Inbox" description="Approve is a governed review effect; it is not runtime authority by itself."><Rows items={data.decisions} render={x=><><div style={{display:"flex",gap:7,alignItems:"center",flexWrap:"wrap"}}><strong style={{fontSize:12}}>{x.decision_key}</strong><Badge kind={x.hypothetical_authority_result==="deny"?"bad":x.hypothetical_authority_result==="escalate"?"warn":"good"}>{x.hypothetical_authority_result}</Badge><Badge>{x.state}</Badge></div><div style={{marginTop:6,fontSize:11}}>{short(x.proposed_action,130)}</div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>{x.authority_reason} · trace {x.trace_id}</div>{["proposed","awaiting_review","revise"].includes(x.state)&&<div style={{display:"flex",gap:6,marginTop:9,flexWrap:"wrap"}}><button onClick={()=>void review(x.id,"approved")} style={{...hqButtonStyle,minHeight:36,color:C.green}}>Approve judgment</button><button onClick={()=>void review(x.id,"revise")} style={{...hqButtonStyle,minHeight:36,color:C.violet}}>Revise</button><button onClick={()=>void review(x.id,"rejected")} style={{...hqButtonStyle,minHeight:36,color:C.red}}>Reject</button></div>}</>}/></HQPanel>}
   {tab==="Skills"&&<HQPanel title="Skill Registry" description="Uncertified skill = zero autonomous execution authority."><Rows items={data.skills} render={x=><><div style={{display:"flex",gap:7,flexWrap:"wrap",alignItems:"center"}}><strong style={{fontSize:12}}>{x.skill_key}@{x.version}</strong><Badge kind={x.certification_status==="certified"?"good":x.certification_status==="revoked"?"bad":"warn"}>{x.certification_status}</Badge><Badge>R{x.risk_class} · L{x.autonomy_required}</Badge></div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>Attempts ≤ {x.max_attempts} · runtime ≤ {x.max_runtime_ms} ms · expires {when(x.expires_at)}</div></>}/></HQPanel>}
   {tab==="Authority"&&<HQPanel title="Authority decisions" description="Allowed and denied requests with scope and reason."><Rows items={data.authority} render={x=><><div style={{display:"flex",gap:7,flexWrap:"wrap"}}><Badge kind={x.decision==="deny"?"bad":"good"}>{x.decision}</Badge><strong style={{fontSize:11.5}}>{x.worker_key}</strong><Badge>{x.skill_key}</Badge><Badge>R{x.risk_class} · L{x.autonomy_level}</Badge></div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>{x.reason_code} · {x.scope_type} · {when(x.occurred_at)}</div></>}/></HQPanel>}
   {tab==="Evidence"&&<HQPanel title="Evidence & provenance"><Rows items={data.evidence} render={x=><><div style={{display:"flex",gap:7,flexWrap:"wrap"}}><Badge>{x.evidence_kind}</Badge><Badge kind={String(x.classification).includes("sensitive")?"warn":"info"}>{x.classification}</Badge><strong style={{fontSize:11}}>{x.source_type}</strong></div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>Trace {x.trace_id} · source {x.source_ref??"—"} · {when(x.created_at)}</div></>}/></HQPanel>}
   {tab==="Failures"&&<div style={{display:"grid",gap:12}}><HQPanel title="Worker Engine anomalies"><Rows items={data.failures.anomalies} render={x=><><div style={{display:"flex",gap:7,flexWrap:"wrap"}}><Badge kind={x.severity==="critical"?"bad":"warn"}>{x.severity}</Badge><Badge>{x.action}</Badge><strong style={{fontSize:11.5}}>{x.anomaly_key}</strong></div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>{short(x.details)} · {when(x.created_at)}</div></>}/></HQPanel><HQPanel title="Dead-letter work"><Rows items={data.failures.dead_letters} render={x=><><strong style={{fontSize:11.5}}>{x.error_code}</strong><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>{x.worker_key} · attempts {x.attempts} · {short(x.error_detail)}</div></>}/></HQPanel></div>}
   {tab==="Resources"&&<HQPanel title="Resource governance"><Rows items={data.resources} render={x=><><div style={{display:"flex",gap:7,flexWrap:"wrap"}}><Badge>{x.resource_kind}</Badge><strong style={{fontSize:11.5}}>{x.amount} {x.unit}</strong>{x.worker_key&&<Badge>{x.worker_key}</Badge>}</div><div style={{marginTop:5,fontSize:10.5,color:C.muted}}>Window {when(x.window_started_at)} · recorded {when(x.recorded_at)}</div></>}/></HQPanel>}
  </>}
 </HQPage>
}
