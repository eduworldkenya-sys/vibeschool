"use client"
export const dynamic="force-dynamic"
import {useCallback,useEffect,useMemo,useState} from "react"
import {supabase} from "@/lib/supabase"
import {HQPage,HQPanel,HQ_THEME as C,hqButtonStyle} from "@/components/hq/HQShell"
import Task15OwnerControls from "./Task15OwnerControls"
type Obj=Record<string,any>
type Snapshot={generated_at:string;engine:Obj|null;counts:Obj;workers:Obj[];jobs:Obj[];shadow_candidates:Obj[];shadow_runs:Obj[];decisions:Obj[];skills:Obj[];authority:Obj[];evidence:Obj[];failures:{anomalies:Obj[];dead_letters:Obj[]};resources:Obj[]}
type Tab="Control Room"|"Workers"|"Jobs"|"Shadow Runs"|"Decisions"|"Skills"|"Authority"|"Evidence"|"Failures"|"Resources"
const sb=supabase as any,tabs:Tab[]=["Control Room","Workers","Jobs","Shadow Runs","Decisions","Skills","Authority","Evidence","Failures","Resources"]
const yes=(v:any)=>v===true,tone=(k:string)=>k==="bad"?C.red:k==="warn"?C.amber:k==="good"?C.green:C.blue,when=(v:any)=>v?new Date(String(v)).toLocaleString("en-KE"):"—",short=(v:any,n=100)=>{const s=typeof v==="string"?v:JSON.stringify(v??{});return s.length>n?s.slice(0,n)+"…":s}
function Badge({children,kind="info"}:{children:React.ReactNode;kind?:string}){return <span style={{display:"inline-flex",padding:"5px 8px",borderRadius:999,border:`1px solid ${tone(kind)}44`,color:tone(kind),fontSize:10,fontWeight:900}}>{children}</span>}
function Metric({label,value,kind="info"}:{label:string;value:any;kind?:string}){return <div style={{padding:13,border:`1px solid ${C.border}`,borderRadius:14,background:C.panelSoft}}><div style={{fontSize:21,fontWeight:950,color:tone(kind)}}>{String(value??0)}</div><div style={{marginTop:4,fontSize:10.5,color:C.muted}}>{label}</div></div>}
function Rows({items,render}:{items:Obj[];render:(x:Obj)=>React.ReactNode}){return <div style={{display:"grid",gap:8}}>{items.length?items.map((x,i)=><div key={String(x.id??x.trace_id??x.worker_key??x.skill_key??i)} style={{padding:12,borderBottom:i===items.length-1?"none":`1px solid ${C.border}`}}>{render(x)}</div>):<div style={{padding:24,textAlign:"center",fontSize:11.5,color:C.muted}}>No records.</div>}</div>}
export default function WorkerEngineControlRoom(){
 const[data,setData]=useState<Snapshot|null>(null),[tab,setTab]=useState<Tab>("Control Room"),[loading,setLoading]=useState(true),[error,setError]=useState("")
 const load=useCallback(async()=>{setLoading(true);setError("");try{const r=await sb.rpc("hq_workforce_get_control_room_snapshot",{p_recent_limit:50});if(r.error)throw r.error;setData(r.data as Snapshot)}catch(e){setError(e instanceof Error?e.message:"Worker Engine Control Room could not be loaded.")}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load])
 const e=data?.engine??{},safety=useMemo(()=>({runtimeOff:!yes(e.runtime_execution_enabled)&&Number(e.runtime_autonomy_level??0)===0,shadowStopped:!yes(e.shadow_enabled)&&!yes(e.shadow_scheduler_enabled)&&yes(e.shadow_global_stop),factoryOff:!yes(e.factory_enabled),heartbeatOff:!yes(e.heartbeat_enabled)}),[e])
 async function review(id:string,state:"approved"|"rejected"|"revise"){const rationale=window.prompt(state==="approved"?"Optional review rationale":"Review rationale")??"";if(state!=="approved"&&!rationale.trim())return;const r=await sb.rpc("hq_workforce_owner_review_shadow_decision",{p_decision_id:id,p_state:state,p_rationale:rationale||null});if(r.error)setError(r.error.message);else await load()}
 return <HQPage title="Worker Engine Control Room" description="Owner-governed digital workforce: observe, verify, authorize, contain and investigate" actions={<button onClick={()=>void load()} disabled={loading} style={hqButtonStyle}>{loading?"Refreshing…":"Refresh"}</button>}>
 {error&&<div role="alert" style={{marginBottom:12,padding:12,border:`1px solid ${C.red}55`,borderRadius:11,color:C.red}}>{error}</div>}
 <div style={{display:"flex",gap:6,overflowX:"auto",paddingBottom:10}}>{tabs.map(t=><button key={t} onClick={()=>setTab(t)} style={{...hqButtonStyle,minHeight:40,whiteSpace:"nowrap",color:tab===t?C.green:C.muted}}>{t}</button>)}</div>
 {!data?<HQPanel><div style={{padding:28,color:C.muted}}>{loading?"Loading governed runtime state…":"No snapshot available."}</div></HQPanel>:<>
 {tab==="Control Room"&&<div style={{display:"grid",gap:12}}><Task15OwnerControls/><section style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(130px,1fr))",gap:8}}><Metric label="Workers" value={data.counts.workers}/><Metric label="Active workers" value={data.counts.active_workers}/><Metric label="Open work" value={data.counts.open_work_items}/><Metric label="Review decisions" value={data.counts.decisions_waiting} kind={Number(data.counts.decisions_waiting)>0?"warn":"good"}/><Metric label="Open anomalies" value={data.counts.open_anomalies} kind={Number(data.counts.open_anomalies)>0?"bad":"good"}/></section><HQPanel title="Safety kernel"><div style={{padding:14,display:"flex",gap:8,flexWrap:"wrap"}}><Badge kind={safety.runtimeOff?"good":"bad"}>Runtime {safety.runtimeOff?"L0 / OFF":"ACTIVE"}</Badge><Badge kind={safety.shadowStopped?"good":"bad"}>Shadow {safety.shadowStopped?"STOPPED":"ENABLED"}</Badge><Badge kind={safety.factoryOff?"good":"bad"}>Factory {safety.factoryOff?"OFF":"ON"}</Badge><Badge kind={safety.heartbeatOff?"good":"bad"}>Heartbeat {safety.heartbeatOff?"OFF":"ON"}</Badge></div></HQPanel></div>}
 {tab==="Workers"&&<HQPanel title="Workers"><Rows items={data.workers} render={x=><><strong>{x.title}</strong> <Badge kind={x.status==="active"?"good":"warn"}>{x.status}</Badge><div style={{fontSize:10.5,color:C.muted}}>{x.worker_key} · {x.department_key} · {when(x.updated_at)}</div></>}/></HQPanel>}
 {tab==="Jobs"&&<HQPanel title="Detected operational work"><Rows items={data.jobs} render={x=><><strong>{x.title}</strong> <Badge>{x.status}</Badge><div style={{fontSize:10.5,color:C.muted}}>{x.department_key} · {x.work_type} · due {when(x.due_at)}</div></>}/></HQPanel>}
 {tab==="Shadow Runs"&&<HQPanel title="Trace runs"><Rows items={data.shadow_runs} render={x=><><strong>{x.trace_id}</strong> <Badge>{x.status}</Badge>{x.consequential_action_performed?<Badge kind="bad">CONSEQUENTIAL WRITE</Badge>:<Badge kind="good">No production action</Badge>}<div style={{fontSize:10.5,color:C.muted}}>{x.worker_key} · {when(x.started_at)}</div></>}/></HQPanel>}
 {tab==="Decisions"&&<HQPanel title="Shadow Decision Inbox" description="Approve is judgment agreement, not execution authority."><Rows items={data.decisions} render={x=><><strong>{x.decision_key}</strong> <Badge>{x.state}</Badge><div style={{fontSize:11}}>{short(x.proposed_action,130)}</div>{["proposed","awaiting_review","revise"].includes(x.state)&&<div style={{display:"flex",gap:6,marginTop:8}}><button onClick={()=>void review(x.id,"approved")} style={hqButtonStyle}>Approve judgment</button><button onClick={()=>void review(x.id,"rejected")} style={hqButtonStyle}>Reject</button></div>}</>}/></HQPanel>}
 {tab==="Skills"&&<HQPanel title="Skill Registry"><Rows items={data.skills} render={x=><><strong>{x.skill_key}@{x.version}</strong> <Badge>{x.certification_status}</Badge><div style={{fontSize:10.5,color:C.muted}}>R{x.risk_class} · L{x.autonomy_required}</div></>}/></HQPanel>}
 {tab==="Authority"&&<HQPanel title="Authority decisions"><Rows items={data.authority} render={x=><><Badge kind={x.decision==="deny"?"bad":"good"}>{x.decision}</Badge> <strong>{x.worker_key}</strong><div style={{fontSize:10.5,color:C.muted}}>{x.reason_code} · {x.scope_type}</div></>}/></HQPanel>}
 {tab==="Evidence"&&<HQPanel title="Evidence & provenance"><Rows items={data.evidence} render={x=><><Badge>{x.evidence_kind}</Badge> <strong>{x.source_type}</strong><div style={{fontSize:10.5,color:C.muted}}>Trace {x.trace_id} · {when(x.created_at)}</div></>}/></HQPanel>}
 {tab==="Failures"&&<div style={{display:"grid",gap:12}}><HQPanel title="Shadow anomalies"><Rows items={data.failures.anomalies} render={x=><><Badge kind="warn">{x.severity}</Badge> <strong>{x.anomaly_key}</strong><div style={{fontSize:10.5,color:C.muted}}>{short(x.details)}</div></>}/></HQPanel><HQPanel title="Dead-letter work"><Rows items={data.failures.dead_letters} render={x=><><strong>{x.error_code}</strong><div style={{fontSize:10.5,color:C.muted}}>{x.worker_key} · {short(x.error_detail)}</div></>}/></HQPanel></div>}
 {tab==="Resources"&&<HQPanel title="Resource governance"><Rows items={data.resources} render={x=><><Badge>{x.resource_kind}</Badge> <strong>{x.amount} {x.unit}</strong><div style={{fontSize:10.5,color:C.muted}}>{when(x.recorded_at)}</div></>}/></HQPanel>}
 </>}
 </HQPage>
}