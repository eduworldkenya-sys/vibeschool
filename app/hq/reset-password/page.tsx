"use client"
export const dynamic = "force-dynamic"

import { useEffect, useMemo, useState } from "react"
import { useRouter } from "next/navigation"
import { hqSupabase } from "@/lib/hq/supabase"

const inputStyle: React.CSSProperties={width:"100%",boxSizing:"border-box",marginTop:7,padding:"13px 14px",borderRadius:12,border:"1px solid rgba(255,255,255,.12)",background:"rgba(255,255,255,.05)",color:"white",fontSize:15,outline:"none"}

export default function HQResetPasswordPage(){
  const router=useRouter();const[password,setPassword]=useState("");const[confirmPassword,setConfirmPassword]=useState("");const[showPassword,setShowPassword]=useState(false);const[ready,setReady]=useState(false);const[checking,setChecking]=useState(true);const[error,setError]=useState("");const[success,setSuccess]=useState("");const[loading,setLoading]=useState(false)
  const checks=useMemo(()=>({length:password.length>=12,letter:/[A-Za-z]/.test(password),number:/\d/.test(password),match:password.length>0&&password===confirmPassword}),[password,confirmPassword]);const validPassword=checks.length&&checks.letter&&checks.number&&checks.match

  useEffect(()=>{let mounted=true;async function establishRecoverySession(){setChecking(true);setError("");try{const code=new URL(window.location.href).searchParams.get("code");if(code){const{error:exchangeError}=await hqSupabase.auth.exchangeCodeForSession(code);if(exchangeError)throw exchangeError;const cleanUrl=new URL(window.location.href);cleanUrl.searchParams.delete("code");window.history.replaceState({},"",`${cleanUrl.pathname}${cleanUrl.search}${cleanUrl.hash}`)}const{data,error:userError}=await hqSupabase.auth.getUser();if(!mounted)return;if(userError||!data.user){setReady(false);setError("This recovery link is invalid, expired or already used. Ask the Founder to send a new password setup/reset email from HQ Team.");return}setReady(true)}catch(e){if(!mounted)return;setReady(false);setError(e instanceof Error?e.message:"The recovery link could not be verified.")}finally{if(mounted)setChecking(false)}}const{data:listener}=hqSupabase.auth.onAuthStateChange((event,session)=>{if(!mounted)return;if(event==="PASSWORD_RECOVERY"&&session?.user){setReady(true);setChecking(false);setError("")}});void establishRecoverySession();return()=>{mounted=false;listener.subscription.unsubscribe()}},[])

  async function handleReset(){setError("");setSuccess("");if(!ready)return setError("A valid recovery session is required.");if(!checks.length)return setError("Use at least 12 characters for the new password.");if(!checks.letter||!checks.number)return setError("Use a password containing both letters and numbers.");if(!checks.match)return setError("Passwords do not match.");setLoading(true);try{const{error:updateError}=await hqSupabase.auth.updateUser({password,data:{hq_password_ready:true}});if(updateError)throw updateError;const{data:access,error:accessError}=await hqSupabase.rpc("hq_check_owner_access",{p_surface:"/hq/reset-password"});const allowed=!accessError&&Boolean((access as{allowed?:boolean}|null)?.allowed);await hqSupabase.auth.signOut({scope:"local"});if(!allowed)throw new Error("Password changed, but this account is not authorized for VibeSchool HQ.");setReady(false);setPassword("");setConfirmPassword("");setSuccess("Password created successfully. Your HQ account is ready. Sign in with this email and your new password.");window.setTimeout(()=>router.replace("/hq/login?reset=success"),900)}catch(e){setError(e instanceof Error?e.message:"Password could not be changed.");setLoading(false)}}
  const disabled=checking||!ready||loading||Boolean(success)

  return <main style={{minHeight:"100dvh",display:"grid",placeItems:"center",background:"radial-gradient(circle at top,#10233d 0,#07111f 46%,#040a12 100%)",color:"#f8fafc",padding:"24px 16px",fontFamily:"Inter,system-ui,sans-serif"}}><section style={{width:"100%",maxWidth:440,border:"1px solid rgba(255,255,255,.1)",borderRadius:24,padding:"clamp(22px,6vw,34px)",background:"rgba(7,17,31,.88)",boxShadow:"0 24px 80px rgba(0,0,0,.28)"}}><div style={{fontSize:11,fontWeight:950,letterSpacing:".13em",color:"#34d399",textTransform:"uppercase",marginBottom:10}}>VibeSchool HQ secure recovery</div><h1 style={{fontSize:"clamp(27px,7vw,34px)",letterSpacing:"-.03em",margin:"0 0 7px"}}>Choose your HQ password</h1><p style={{fontSize:13,lineHeight:1.6,color:"rgba(255,255,255,.52)",margin:"0 0 23px"}}>Only you choose and know this password. Completing this page marks your HQ account as ready for sign-in.</p>{checking&&<div role="status" style={{padding:12,borderRadius:11,marginBottom:16,background:"rgba(96,165,250,.08)",border:"1px solid rgba(96,165,250,.22)",color:"#bfdbfe",fontSize:12}}>Verifying secure recovery link…</div>}{error&&<div role="alert" style={{padding:12,borderRadius:11,marginBottom:16,background:"rgba(239,68,68,.1)",border:"1px solid rgba(239,68,68,.25)",color:"#fecaca",fontSize:12,lineHeight:1.55}}>{error}</div>}{success&&<div role="status" style={{padding:12,borderRadius:11,marginBottom:16,background:"rgba(52,211,153,.08)",border:"1px solid rgba(52,211,153,.25)",color:"#bbf7d0",fontSize:12,lineHeight:1.55}}>{success}</div>}<div style={{display:"flex",alignItems:"center",justifyContent:"space-between",gap:12}}><label htmlFor="new-password" style={{fontSize:11,fontWeight:800,color:"rgba(255,255,255,.62)"}}>New password</label><button type="button" onClick={()=>setShowPassword(v=>!v)} disabled={disabled} style={{border:0,background:"transparent",padding:0,color:"#93c5fd",fontSize:10.5,fontWeight:800}}>{showPassword?"Hide":"Show"}</button></div><input id="new-password" value={password} onChange={e=>setPassword(e.target.value)} type={showPassword?"text":"password"} autoComplete="new-password" minLength={12} disabled={disabled} style={{...inputStyle,marginBottom:14,opacity:disabled?.58:1}}/><label htmlFor="confirm-password" style={{fontSize:11,fontWeight:800,color:"rgba(255,255,255,.62)"}}>Confirm new password</label><input id="confirm-password" value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&!disabled&&validPassword)void handleReset()}} type={showPassword?"text":"password"} autoComplete="new-password" minLength={12} disabled={disabled} style={{...inputStyle,marginBottom:14,opacity:disabled?.58:1}}/><div style={{display:"grid",gridTemplateColumns:"repeat(2,minmax(0,1fr))",gap:8,marginBottom:21}}>{[[checks.length,"12+ characters"],[checks.letter,"Contains letters"],[checks.number,"Contains a number"],[checks.match,"Passwords match"]].map(([ok,label])=><div key={String(label)} style={{padding:"8px 9px",borderRadius:9,border:`1px solid ${ok?"rgba(52,211,153,.22)":"rgba(255,255,255,.08)"}`,background:ok?"rgba(52,211,153,.07)":"rgba(255,255,255,.025)",color:ok?"#a7f3d0":"rgba(255,255,255,.4)",fontSize:10.5,fontWeight:750}}>{ok?"✓":"○"} {String(label)}</div>)}</div><button disabled={disabled||!validPassword} onClick={()=>void handleReset()} style={{width:"100%",padding:14,border:"1px solid rgba(16,185,129,.35)",borderRadius:12,background:disabled||!validPassword?"rgba(16,185,129,.32)":"#10b981",color:"#03120c",fontWeight:950,fontSize:13.5,cursor:disabled||!validPassword?"not-allowed":"pointer"}}>{loading?"Saving password…":"Save password & activate HQ"}</button></section></main>
