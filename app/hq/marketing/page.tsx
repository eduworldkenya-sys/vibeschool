"use client"
import {useEffect,useState} from "react"
import {hqSupabase} from "@/lib/hq/supabase"
import {HQPage,HQPanel,HQ_THEME as C} from "@/components/hq/HQShell"
type M={campaigns:number;active_campaigns:number;events_30d:number;leads_30d:number;conversions_30d:number}
export default function HQMarketing(){const [d,setD]=useState<M|null>(null),[error,setError]=useState("");useEffect(()=>{void(async()=>{const {data,error}=await hqSupabase.rpc("hq_marketing_overview");if(error)setError(error.message);else setD(data as M)})()},[]);return <HQPage title="Marketing intelligence" description="Campaign activity, lead flow and conversion signals. No raw private user data is exposed here.">{error&&<div style={{color:C.red,marginBottom:12}}>{error}</div>}<section style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(160px,1fr))",gap:10}}>{[['Open campaigns',d?.campaigns],['Active',d?.active_campaigns],['Events 30d',d?.events_30d],['Leads 30d',d?.leads_30d],['Conversions 30d',d?.conversions_30d]].map(([a,v])=><HQPanel key={String(a)} title={String(a)}><div style={{padding:14,fontSize:25,fontWeight:900}}>{v??"—"}</div></HQPanel>)}</section><div style={{height:12}}/><HQPanel title="Marketing operating model"><div style={{padding:14,color:C.muted,fontSize:12,lineHeight:1.7}}>Use campaigns to measure acquisition sources, onboarding conversion and paid conversion. Keep consent and purpose boundaries explicit; do not use this surface for unrestricted user surveillance.</div></HQPanel></HQPage>}
