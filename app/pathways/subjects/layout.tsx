import type { Metadata } from 'next'
export const metadata: Metadata = { title:'Senior School Subjects & Tracks', description:'Explore Kenya Senior School pathways, tracks and example three-subject combinations with clear evidence boundaries.', alternates:{canonical:'/pathways/subjects'} }
export default function Layout({children}:{children:React.ReactNode}){return children}
