"use client"

import { useEffect, useMemo, useRef, useState } from "react"
import { useRouter } from "next/navigation"
import { usePublicationDraft } from "@/hooks/usePublicationDraft"
import { getSupabaseClient } from "@/lib/supabase"
import { parseDraftMediaRef, removeVibePressMedia, resolveVibePressMediaUrl, uploadDraftMedia, validateVibePressImage } from "@/lib/vibepressMedia"

const templates = [
  ["Revision guide", "The problem\n\nStart with the learner's real situation.\n\nWhat to understand\n\nExplain the idea clearly and specifically.\n\nTry this now\n\n5 minutes — Diagnose what you can and cannot do.\n15 minutes — Attempt questions before rereading.\n10 minutes — Check and label each mistake.\n10 minutes — Repair only what the mistakes exposed.\n5 minutes — Retest without notes.\n\nWhat to do next\n\nGive one concrete next action the learner can take."],
  ["Exam guide", "The challenge\n\nExplain what the examination tests and why learners lose marks.\n\nHow to answer\n\nGive a clear step-by-step method.\n\nExaminer tip\n\nShow how marks are earned.\n\nPractice question\n\nAdd a question and model answer.\n\nWhat to do next\n\nGive one concrete practice action."],
  ["Education news", "What happened\n\nState the verified development.\n\nWhy it matters\n\nExplain the impact on learners, teachers or parents.\n\nWhat to do next\n\nGive practical, clearly-labelled guidance.\n\nSources\n\nAdd links to the original authoritative sources."],
] as const

export function SimpleArticleEditor({authorId, publicationId}:{authorId:string;publicationId?:string}){
  const router=useRouter(); const draft=usePublicationDraft(authorId,"vibepress",publicationId); const [publishing,setPublishing]=useState(false)
  const [uploadingCover,setUploadingCover]=useState(false); const [coverError,setCoverError]=useState<string|null>(null); const [coverPreview,setCoverPreview]=useState<string|null>(null); const coverInputRef=useRef<HTMLInputElement>(null)
  const chapter=draft.chapters.find(item=>item.id===draft.activeChapterId)??draft.chapters[0]
  const body=useMemo(()=>chapter?.blocks.map(block=>block.content).filter(Boolean).join("\n\n")??"",[chapter]); const bodyBlock=chapter?.blocks[0]
  const wordCount=body.trim()?body.trim().split(/\s+/).length:0
  function setBody(value:string){if(bodyBlock)draft.updateBlock(bodyBlock.id,value)}
  function applyTemplate(value:string){if(body.trim()&&!window.confirm("Replace the current article body with this template?"))return;setBody(value)}
  async function publish(){const publication=draft.publication;if(!publication)return;const checks=publicationChecks(publication.title??"",publication.description??"",publication.tags??[],body);if(checks.some(check=>!check.pass))return;setPublishing(true);const saved=await draft.forceSave();const published=saved&&await draft.publishPublication();setPublishing(false);if(published)router.push(`/blog/${draft.publication?.id}`)}

  useEffect(()=>{
    let cancelled=false
    const value=draft.publication?.cover_url
    if(!value){setCoverPreview(null);return()=>{cancelled=true}}
    if(!parseDraftMediaRef(value)){setCoverPreview(value);return()=>{cancelled=true}}
    void resolveVibePressMediaUrl(getSupabaseClient(),value).then(url=>{if(!cancelled)setCoverPreview(url)}).catch(()=>{if(!cancelled)setCoverError("Cover preview could not be loaded.")})
    return()=>{cancelled=true}
  },[draft.publication?.cover_url])

  async function uploadCover(event:React.ChangeEvent<HTMLInputElement>){
    const file=event.target.files?.[0]
    if(!file)return
    const validationError=validateVibePressImage(file)
    if(validationError){setCoverError(validationError.replace("Image","Cover image"));event.target.value="";return}
    setUploadingCover(true);setCoverError(null)
    try{
      const sb=getSupabaseClient();const{data:{user}}=await sb.auth.getUser();if(!user)throw new Error("Sign in before uploading a cover image")
      const previous=draft.publication?.cover_url
      const draftRef=await uploadDraftMedia(sb,user.id,"covers",file)
      const preview=await resolveVibePressMediaUrl(sb,draftRef)
      draft.updatePublication({cover_url:draftRef});setCoverPreview(preview)
      if(parseDraftMediaRef(previous))void removeVibePressMedia(sb,previous)
    }catch(error){setCoverError(error instanceof Error?error.message:"Cover upload failed. Try again.")}
    finally{setUploadingCover(false);if(coverInputRef.current)coverInputRef.current.value=""}
  }

  if(draft.loading)return <main className="simple-loading">Opening your article…</main>
  if(!draft.publication||!chapter)return <main className="simple-loading">The editor could not open. {draft.error}</main>
  const pub=draft.publication;const checks=publicationChecks(pub.title??"",pub.description??"",pub.tags??[],body);const publishReady=checks.every(check=>check.pass)
  return <main className="simple-editor"><header><button onClick={()=>router.push("/hq/studio")}>← Studio</button><div><strong>VibeSchool Article Editor</strong><span>{draft.saving?"Saving…":draft.lastSaved?"Saved automatically":"Draft"}</span></div><button className="publish" disabled={publishing||!publishReady} onClick={publish}>{publishing?"Publishing…":"Publish"}</button></header><div className="workspace">
    <section className="templates"><p>START FAST</p><h2>Choose a structure</h2>{templates.map(([name,value])=><button key={name} onClick={()=>applyTemplate(value)}><strong>{name}</strong><span>Use template →</span></button>)}</section>
    <section className="canvas"><label>Headline<input autoFocus value={pub.title??""} onChange={event=>draft.updatePublication({title:event.target.value})} placeholder="A clear, useful headline"/></label><label>Short introduction<textarea rows={3} value={pub.description??""} onChange={event=>draft.updatePublication({description:event.target.value,subtitle:event.target.value})} placeholder="Tell the reader what they will gain."/></label><div className="row"><label>Category<select value={pub.genre} onChange={event=>draft.updatePublication({genre:event.target.value as typeof pub.genre})}><option value="academic">Revision</option><option value="magazine">Education news</option><option value="self_help">Study skills</option><option value="non_fiction">Careers & guidance</option></select></label><label>Cover image<div className="coverControl"><button type="button" onClick={()=>coverInputRef.current?.click()} disabled={uploadingCover}>{uploadingCover?"Uploading…":pub.cover_url?"Change image":"Upload image"}</button><input value={parseDraftMediaRef(pub.cover_url)?"Uploaded image":pub.cover_url??""} onChange={event=>draft.updatePublication({cover_url:event.target.value||null})} placeholder="Or paste an image URL" disabled={Boolean(parseDraftMediaRef(pub.cover_url))}/><input ref={coverInputRef} className="hiddenFile" type="file" accept="image/jpeg,image/png,image/webp,image/gif" onChange={uploadCover}/></div><small>JPEG, PNG, WebP or GIF · max 5 MB · stored privately until publication</small></label></div>{coverError&&<p className="error">{coverError}</p>}<label>Tags<input value={(pub.tags??[]).join(", ")} onChange={event=>draft.updatePublication({tags:event.target.value.split(",").map(tag=>tag.trim()).filter(Boolean)})} placeholder="KCSE, Chemistry, Revision"/></label><label>Article body<textarea className="body" rows={22} value={body} onChange={event=>setBody(event.target.value)} placeholder="Write naturally. Include a clear 'What to do next' section."/></label><section className="readiness" aria-label="Publishing readiness"><div><strong>Ready to publish?</strong><span>{wordCount} words</span></div>{checks.map(check=><p className={check.pass?"pass":"todo"} key={check.label}>{check.pass?"✓":"○"} {check.label}</p>)}</section>{!pub.cover_url&&<p className="advice">Social preview: upload a cover image for a stronger WhatsApp and social card.</p>}{draft.error&&<p className="error">{draft.error}</p>}</section>
    <aside><p>MOBILE PREVIEW</p><div className="preview">{coverPreview&&<img src={coverPreview} alt=""/>}<small>VIBESCHOOL EDUCATION</small><h1>{pub.title||"Your headline appears here"}</h1><b>{pub.description||"Your short introduction appears here."}</b>{body.split(/\n\n+/).filter(Boolean).slice(0,6).map((text,index)=><p key={index}>{text}</p>)}</div></aside>
  </div><style jsx>{`*{box-sizing:border-box}.simple-editor{min-height:100dvh;background:#f4f1e8;color:#111827;font-family:var(--font-jakarta),Arial,sans-serif}.simple-loading{min-height:100dvh;display:grid;place-items:center;background:#101827;color:white}.simple-editor>header{position:sticky;top:0;z-index:5;min-height:62px;padding:10px 18px;background:#07111f;color:white;display:flex;align-items:center;justify-content:space-between;gap:14px}.simple-editor header button{border:0;background:transparent;color:#cbd5e1;font-weight:800;cursor:pointer}.simple-editor header div{display:grid;text-align:center}.simple-editor header span{font-size:11px;color:#94a3b8}.simple-editor header .publish{background:#d0b154;color:#07111f;border-radius:9px;padding:10px 16px}.simple-editor header .publish:disabled{opacity:.45}.workspace{max-width:1450px;margin:auto;padding:22px;display:grid;grid-template-columns:210px minmax(0,760px) minmax(260px,1fr);gap:18px}.templates,.canvas,aside{background:#fff;border:1px solid #dedbd2;border-radius:16px;padding:18px;align-self:start}.templates>p,aside>p{font-size:10px;letter-spacing:.14em;font-weight:900;color:#755b17}.templates h2{font-size:20px}.templates button{width:100%;text-align:left;display:grid;gap:4px;padding:13px;margin:8px 0;border:1px solid #ddd8ca;border-radius:10px;background:#faf8f2;cursor:pointer}.templates span{font-size:11px;color:#755b17}.canvas{display:grid;gap:17px}.canvas label{display:grid;gap:7px;font-size:12px;font-weight:850}.canvas input,.canvas textarea,.canvas select{width:100%;border:1px solid #ccd0d6;border-radius:10px;padding:11px 12px;font:inherit;background:white}.canvas input:first-of-type{font-size:20px;font-weight:850}.canvas .body{line-height:1.7;resize:vertical}.row{display:grid;grid-template-columns:1fr 1fr;gap:12px}.coverControl{display:grid!important;grid-template-columns:auto 1fr!important;gap:8px}.coverControl button{border:1px solid #b79b45!important;background:#f8f1d8!important;color:#5e4910!important;border-radius:10px!important;padding:10px 12px!important;font-weight:850!important;cursor:pointer!important}.coverControl button:disabled{opacity:.55}.hiddenFile{display:none!important}.canvas label small{font-size:10px;font-weight:600;color:#6b7280}.readiness{border:1px solid #ddd8ca;border-radius:12px;padding:14px;background:#faf8f2}.readiness>div{display:flex;justify-content:space-between;gap:12px}.readiness>div span{font-size:12px;color:#6b7280}.readiness p{margin:8px 0 0;font-size:12px}.readiness .pass{color:#166534}.readiness .todo{color:#92400e}.advice{margin:0;color:#755b17;font-size:12px}.preview{border:1px solid #ddd8ca;border-radius:16px;overflow:hidden;padding:18px;font-family:Georgia,serif}.preview img{width:calc(100% + 36px);height:150px;object-fit:cover;margin:-18px -18px 15px}.preview small{color:#755b17;font:800 9px Arial;letter-spacing:.12em}.preview h1{font-size:28px;line-height:1.05;margin:8px 0 12px}.preview b{font:600 13px/1.5 Arial;color:#58606c}.preview p{white-space:pre-wrap;line-height:1.65;color:#374151}.error{color:#b91c1c;font-weight:700}@media(max-width:1050px){.workspace{grid-template-columns:190px 1fr}.workspace aside{grid-column:1/-1}.preview{max-width:680px;margin:auto}}@media(max-width:720px){.simple-editor>header{padding:8px 10px}.simple-editor header div strong{font-size:13px}.workspace{padding:10px;grid-template-columns:1fr}.templates{display:flex;overflow-x:auto;gap:8px}.templates>p,.templates h2{display:none}.templates button{min-width:155px;margin:0}.row{grid-template-columns:1fr}.coverControl{grid-template-columns:1fr!important}.canvas{padding:15px}.workspace aside{display:none}}`}</style></main>
}

function publicationChecks(title:string,description:string,tags:string[],body:string){const words=body.trim()?body.trim().split(/\s+/).length:0;return[{label:'Clear headline (12+ characters)',pass:title.trim().length>=12},{label:'Useful introduction (60+ characters)',pass:description.trim().length>=60},{label:'At least one discovery tag',pass:tags.some(tag=>tag.trim().length>0)},{label:'Substantial article body (150+ words)',pass:words>=150},{label:'A concrete next step for the reader',pass:/what to do next|before you leave|try this|start here|next step/i.test(body)}]}
