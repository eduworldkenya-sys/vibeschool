"use client";
import { useEffect,useState } from "react";
import { LearningTransformPanel } from "@/components/read/LearningTransformPanel";
type Detail={publicationId?:unknown;chapterId?:unknown};
export function ReaderLearningLauncher(){const[source,setSource]=useState<{chapterId:string;chapterTitle:string}|null>(null);const[open,setOpen]=useState(false);useEffect(()=>{function chapter(event:Event){const d=(event as CustomEvent<Detail>).detail;if(typeof d?.chapterId!=="string")return;const heading=document.querySelector("#reader-active-unit h1,#reader-active-unit h2")?.textContent?.trim();setSource({chapterId:d.chapterId,chapterTitle:heading||"This unit"})}function launch(){setOpen(true)}window.addEventListener("vibe:reader-chapter",chapter);window.addEventListener("vibe:reader-open-learn",launch);return()=>{window.removeEventListener("vibe:reader-chapter",chapter);window.removeEventListener("vibe:reader-open-learn",launch)}},[]);if(!source||!open)return null;return <div role="dialog" aria-modal="true" aria-label="Learn with this unit" onMouseDown={e=>{if(e.currentTarget===e.target)setOpen(false)}} style={{position:"fixed",inset:0,zIndex:100,background:"rgba(0,0,0,.68)",display:"flex",alignItems:"flex-end",justifyContent:"center"}}><div style={{width:"100%",maxWidth:720,maxHeight:"86dvh",overflowY:"auto",background:"#111318",borderRadius:"22px 22px 0 0",padding:18,boxSizing:"border-box",color:"white"}}><div style={{display:"flex",justifyContent:"space-between",gap:12}}><div><strong>Learn</strong><div style={{fontSize:12,opacity:.65,marginTop:3}}>{source.chapterTitle}</div></div><button type="button" aria-label="Close learning tools" onClick={()=>setOpen(false)} style={{border:0,background:"transparent",color:"white",fontSize:24}}>×</button></div><LearningTransformPanel chapterId={source.chapterId} chapterTitle={source.chapterTitle}/></div></div>}
