"use client"

import React,{useEffect,useState}from'react'
import{createBrowserClient}from'@supabase/ssr'
const SURF='#111827',CARD='#1a2235',TEXT='#fff',MUTED='rgba(255,255,255,.48)',ACCENT='#CCFF00',BORDER='rgba(255,255,255,.08)'
type Revision={id:string;revision_number:number;reason:string|null;created_at:string}
export function PublicationHistoryDrawer({publicationId,isOpen,onClose}:{publicationId:string;isOpen:boolean;onClose:()=>void}){const[items,setItems]=useState<Revision[]>([]);const[loading,setLoading]=useState(false);const[error,setError]=useState('');useEffect(()=>{if(!isOpen||!publicationId)return;let alive=true;setLoading(true);setError('');const sb=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);void sb.from('publication_revisions').select('id,revision_number,reason,created_at').eq('publication_id',publicationId).order('revision_number',{ascending:false}).then(({data,error:queryError})=>{if(!alive)return;if(queryError)setError(queryError.message);else setItems((data??[])as Revision[]);setLoading(false)});return()=>{alive=false}},[isOpen,publicationId]);if(!isOpen)return null;return <><button type="button" aria-label="Close revision history" onClick={onClose} style={{position:'fixed',inset:0,background:'rgba(0,0,0,.68)',border:0,zIndex:300}}/><aside style={{position:'fixed',inset:'0 0 0 auto',width:'min(92vw,420px)',background:SURF,borderLeft:`1px solid ${BORDER}`,zIndex:310,padding:'20px 16px 32px',overflowY:'auto',boxSizing:'border-box'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:12,marginBottom:18}}><div><div style={{color:ACCENT,fontSize:9,fontWeight:850,letterSpacing:'.12em'}}>PUBLICATION HISTORY</div><h2 style={{color:TEXT,fontSize:20,margin:'5px 0 0'}}>Versions</h2></div><button type="button" onClick={onClose} style={{width:36,height:36,borderRadius:10,border:`1px solid ${BORDER}`,background:CARD,color:TEXT,cursor:'pointer',fontSize:18}}>×</button></div><p style={{color:MUTED,fontSize:12,lineHeight:1.6,margin:'0 0 16px'}}>A snapshot is captured whenever this publication is published or updated. Draft autosaves do not create public versions.</p>{loading&&<div style={{color:MUTED,fontSize:13}}>Loading versions…</div>}{error&&<div style={{color:'#ff7b7b',fontSize:12}}>{error}</div>}{!loading&&!error&&items.length===0&&<div style={{background:CARD,border:`1px solid ${BORDER}`,borderRadius:12,padding:16,color:MUTED,fontSize:13,lineHeight:1.55}}>No published version has been captured yet. Publish or update this book to create Revision 1.</div>}<div style={{display:'grid',gap:9}}>{items.map(item=><div key={item.id} style={{background:CARD,border:`1px solid ${BORDER}`,borderRadius:12,padding:14}}><div style={{display:'flex',justifyContent:'space-between',gap:10}}><div style={{color:TEXT,fontSize:14,fontWeight:800}}>Revision {item.revision_number}</div><time style={{color:MUTED,fontSize:10}}>{new Date(item.created_at).toLocaleString('en-KE',{dateStyle:'medium',timeStyle:'short'})}</time></div><div style={{color:MUTED,fontSize:12,marginTop:6}}>{item.reason||'Published update'}</div></div>)}</div></aside></>}
