# Merge policy

Never merge on repository intent alone. Merge the replacement only at an exact reviewed head with successful applicable checks. Use the expected head SHA when performing the merge to prevent race-condition certification drift.
