# Acceptance

This change is complete only when exact-head CI passes and the branch remains current with main. Post-merge production verification must confirm `/hq/content` renders publication focus, Chemistry chapter grouping, semantic non-review actions, and exact-version governed artifact decisions.
