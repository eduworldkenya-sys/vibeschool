# Completion gate

The PR #445 reconciliation is complete only when the replacement branch is reviewed against current main, all applicable exact-head GitHub Actions checks are successful, the dedicated governed proof executes successfully, the proof-contract/escape-hatch checks pass, the replacement PR is merged, and PR #445 is then closed as superseded.
