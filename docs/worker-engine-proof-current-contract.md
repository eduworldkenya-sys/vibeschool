# Current contract alignment

The current governed runtime admits execution only for active or restricted workers and only for configured execution modes. The replacement proof therefore uses an active canonical fixture for downstream control tests and separately proves that probation workers and unconfigured modes fail closed.
