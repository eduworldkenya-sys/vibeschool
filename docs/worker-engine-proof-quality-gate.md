# Quality gate

A green build without executed adversarial tests is insufficient. A passing adversarial suite with forbidden type/test escape hatches is insufficient. Both behavior proof and contract integrity must pass at the exact head.
