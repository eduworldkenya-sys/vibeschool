// Production mirror: twin-chat v26
// Supabase Edge Function is ACTIVE with verify_jwt=true.
// Student safeguarding checks run before entitlement; normal student Twin requests require
// a stable sessionId and call student_consume_twin_session({ p_session_key: sessionId }).
// Full source is deployed in Supabase and should remain in sync with this contract.
