import { serve } from "https://deno.land/std@0.168.0/http/server.ts"
import { groqText, invokeCyborgEdgeModel } from "../_shared/cyborg-model-client.ts"

const CORS={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"}
serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:CORS});try{const{teacher,school,subject,className,studentCount,duration,topic,focus,previousTopics}=await req.json(),prevList=previousTopics?.length?"Previously covered: "+previousTopics.join(", ")+".":"This is the first recorded lesson for this class.",prompt=["You are an expert Kenyan CBC curriculum lesson planner. Generate a complete practical classroom-ready lesson plan.","Teacher: "+teacher,"School: "+school,"Subject: "+subject,"Class: "+className,"Number of learners: "+studentCount,"Duration: "+duration,"Topic: "+topic,focus?"Teacher focus: "+focus:"",prevList,"","Return ONLY this exact XML with no other text:","<objectives>3 clear measurable CBC learning objectives.</objectives>","<resources>Specific materials needed.</resources>","<introduction>Exact 5-7 minute hook with actual teacher words.</introduction>","<development>Detailed 20-25 minute teaching script with teacher talk, board work, questions and answers, exercises, common mistakes.</development>","<consolidation>8-10 minute wrap-up with cold-call questions.</consolidation>","<assessmentHook>One formative assessment moment.</assessmentHook>","<homework>Specific homework with exact questions.</homework>","<differentiation>Higher: extension task. On track: core task. Support: scaffolding strategy.</differentiation>","<student_notes>3-5 plain English bullet points for parents and students. Include CBC strand.</student_notes>","<parent_message>Complete parent message ready to send. Greeting, what was learned, homework numbered, home tip, sign-off with "+teacher+" and "+school+".</parent_message>"].join("\n"),cyborg=await invokeCyborgEdgeModel({callerServiceId:'edge.swift-processor',actorKey:'edge:swift-processor',externalChatId:`swift:${crypto.randomUUID()}`,objective:`Generate governed CBC lesson plan for ${String(subject??'subject').slice(0,80)}`,provider:'groq',model:'llama-3.3-70b-versatile',maxTokens:4000,messages:[{role:'user',content:prompt}],metadata:{feature:'swift-processor'},dataClassification:'confidential'}),text=groqText(cyborg.output);if(!text)return new Response(JSON.stringify({error:"Empty governed model response"}),{status:500,headers:{...CORS,"Content-Type":"application/json"}});return new Response(JSON.stringify({plan:text,missionId:cyborg.missionId,lineage:cyborg.lineage}),{headers:{...CORS,"Content-Type":"application/json"}})}catch(err){return new Response(JSON.stringify({error:String(err)}),{status:500,headers:{...CORS,"Content-Type":"application/json"}})}})
