# VibeSchool Claude Instructions

Before any repository work, read and obey `AGENTS.md`. It is the canonical model-neutral VibeSchool AI operating contract. These instructions add to it and never weaken it.

When relevant repository procedures exist under `skills/`, read and apply them before planning or editing. CI failures must be investigated and repaired through the error/CI repair loop in `AGENTS.md`; never bypass failing checks or claim certification from stale evidence.

## Timetable fix work

Before working on any timetable task, always read:
- TIMETABLE_FIX_REGISTER.md
- HANDOVER.md

The active fix in HANDOVER.md controls scope.

Never run destructive SQL or migration-history repair against production without explicit approval in the current session.
