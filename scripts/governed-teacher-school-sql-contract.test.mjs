import fs from 'node:fs';import assert from 'node:assert/strict';const sql=fs.readFileSync('supabase/migrations/20260902104000_governed_teacher_school_claims_current.sql','utf8');assert.match(sql,/teacher_school_claim_has_target/);assert.match(sql,/status in \('matched','new'\)/);assert.match(sql,/filter \(where x is not null\)/);assert.match(sql,/on conflict\(school_id,profile_id\) do update set role='teacher'/);console.log('governed teacher-school SQL edge contract: PASS');